import type { ReactNode } from 'react'

export const metadata = {
  title: 'Reset Password — SetupFX',
  description: 'Reset your SetupFX account password.',
}

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
