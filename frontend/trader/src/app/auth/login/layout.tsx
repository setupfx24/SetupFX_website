import type { ReactNode } from 'react'

export const metadata = {
  title: 'Sign In — SetupFX',
  description: 'Sign in to your SetupFX trading account.',
}

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
