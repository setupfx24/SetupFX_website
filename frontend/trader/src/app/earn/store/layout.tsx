import type { ReactNode } from 'react'

export const metadata = {
  title: 'Rewards Store — SwissCresta',
  description: 'Redeem reward points for trading credits and merchandise.',
}

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
