# Dependencies
node_modules/
**/node_modules/

# Next.js
.next/
out/
**/.next/

# Python
venv/
.venv/
**/__pycache__/
*.py[cod]
*$py.class
*.so
.Python
**/venv/

# Env & secrets (never commit)
.env
.env.*
!.env.example
*.pem

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
.cursor/

# Logs & debug
*.log
*.txt.log
admin_logs.txt
npm-debug.log*
yarn-debug.log*

# Upload storage (use object storage in production)
/uploads/
**/uploads/

# Database backups
/backups/

# Test / coverage
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/

# Build
dist/
build/
*.egg-info/

# Misc
*.local
*.tsbuildinfo

# Claude Code agent state (never commit)
.claude/

# Local working notes (extracted client doc text, scratch files)
.clientdocs/

# Binary specs / drafts / office docs — kept locally, not tracked
*.docx
*.doc
*.pdf
*.xlsx
*.xls
*.ppt
*.pptx
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
*.rar
*.bak
~$*

# Client reference mockups dropped at the repo root (kept locally only)
/ChatGPT Image*.png
/ChatGPT Image*.jpg
setupfx_web/

# Source APK drop-zone — redundant with the served copy at
# frontend/trader/public/downloads/setupfx1.apk. Keep locally, don't track.
# (Scoped to the root folder only so the served public/ APK stays tracked.)
/apk/

# Served Android APK — large binary, deployed to the server manually (not via git).
frontend/trader/public/downloads/*.apk

# Raw TradingView library download — the deployed copy lives in
# frontend/trader/public/charting_library (self-hosted). This is the
# unpacked source archive and is not needed in the build.
/charting_library-master/
